# Versions tested on the development machine.

# Core scientific stack
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
scipy>=1.8
matplotlib==3.10.9

# Deep learning (DLinear). Linux PyPI wheel is CUDA-enabled by default.
# CPU-only:  pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2

# Topological data analysis (phlib backends)
ripser==0.6.14
persim==0.3.8
gudhi==3.12.0

# Evolutionary search
deap==1.4.4

# Data download (optional; CSVs are already bundled)
yfinance==1.3.0

# REST API service (serve.py)
flask>=2.0

# Required by vendored TSLib transformer models
einops==0.8.2
